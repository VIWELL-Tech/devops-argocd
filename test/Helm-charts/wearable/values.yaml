name: wearable
replicaCount: 1
AppVersion: "test-169ec0c-68"

image:
  repository: 814880204573.dkr.ecr.us-east-1.amazonaws.com/wearable
  pullPolicy: IfNotPresent
  tag: ''

podAnnotations:
  linkerd.io/inject: disabled

podSecurityContext: {}

service:
  type: NodePort
  port: 30040
  targetport: 30040

# resources:
#   requests:
#     cpu: 250m
#     memory: 128m

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage:
namespace: 'test'

##for liveness and readiness
path: '/health'
port: 30040
