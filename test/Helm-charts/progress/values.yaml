name: progress
replicaCount: 1
AppVersion: "test-91c26be-126"

image:
  repository: 814880204573.dkr.ecr.us-east-1.amazonaws.com/progress
  pullPolicy: IfNotPresent
  tag: ''

podAnnotations:
  linkerd.io/inject: disabled

podSecurityContext: {}

service:
  type: NodePort
  port: 30024
  targetport: 30024

# resources:
#   requests:
#     cpu: 250m
#     memory: 128m

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage:
namespace: 'test'

##for liveness and readiness
path: '/health'
port: 30024
