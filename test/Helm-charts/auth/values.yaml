name: auth
replicaCount: 1
AppVersion: "test-f2f97a2-170"

image:
  repository: 814880204573.dkr.ecr.us-east-1.amazonaws.com/auth
  pullPolicy: IfNotPresent
  tag: ''

podAnnotations:
  linkerd.io/inject: disabled

podSecurityContext: {}

service:
  type: NodePort
  port: 3001
  targetport: 3001

# resources:
#   requests:
#     cpu: 250m
#     memory: 128m

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage:
namespace: 'test'

##for liveness and readiness
path: '/health'
port: 3001
