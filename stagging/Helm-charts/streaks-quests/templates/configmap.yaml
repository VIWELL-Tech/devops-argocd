apiVersion: v1
data:
  ENV: "dev"
  VAR: "INPUT"
  PORT: "30044"
  HOST: "0.0.0.0"
  SQL_DB_NAME: "staging_streak"
  LOG_TOPIC: "staging_log_topic"
  
kind: ConfigMap
metadata:
  name:  {{ .Values.name }}
  namespace: {{ .Values.namespace }}
